import { join } from 'path';

export const PAYMENT_PROTO_PATH = join(__dirname, '../../proto/payment.proto');

export const NOTIFICATION_PROTO_PATH = join(__dirname, '../../proto/notification.proto');

export const BOOKING_PROTO_PATH = join(__dirname, '../../proto/booking.proto');

export const USER_PROTO_PATH = join(__dirname, '../../proto/user.proto');