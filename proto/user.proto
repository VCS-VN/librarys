syntax="proto3";
package user;

service UserService {
    rpc GetUserByAccountId(User) returns (User);
    rpc GetUserById(User) returns (User);
}

message User{
    optional string id = 1;
    optional string phoneNumber = 2;
    optional string accountId = 3;
}