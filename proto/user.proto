syntax="proto3";
package user;

service UserService {
    rpc GetUserByAccountId(UserMessage) returns (UserMessage);
    rpc GetUserById(UserMessage) returns (UserMessage);
}

message UserMessage {
    optional string id = 1;
    optional string phoneNumber = 2;
    optional string accountId = 3;
}